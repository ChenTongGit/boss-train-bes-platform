spring:
  application:
    name: boss-bes-authentication

  cloud:
    nacos:
      config:
        server-addr: ${spring.nacos-url.nacos-host}:${spring.nacos-url.nacos-port}
        file-extension: yaml
  main:
    allow-bean-definition-overriding: true

server:
  port: 4445
