spring:
  nacos-host: 123.57.156.184
  nacos-port: 8848
  cloud:
    alibaba:
      seata:
        tx-service-group: demo1-server-group
    nacos:
      discovery:
        server-addr: ${spring.nacos-host}:${spring.nacos-port}
      config:
        server-addr: ${spring.nacos-host}:${spring.nacos-port}
        file-extension: yaml
  application:
    name: user-seata-service
